version: 0.2

phases:
  install:
    commands:
      - npm install
  build:
    commands:
      - sudo apt install nginx -y
      - cp public/index.html /var/www/html
      - sudo systemctl restart nginx
      
      

artifacts:
  files:
    - '**/*'

